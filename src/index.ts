function splitProxy(proxy: string) {
  const atSign = /@/;
  if (atSign.test(proxy)) {
    return atSign.test(proxy);
  } else {
    return atSign.test(proxy);
  }
}

module.exports = splitProxy;
export default splitProxy;
